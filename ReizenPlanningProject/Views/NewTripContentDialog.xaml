<ContentDialog
    x:Class="ReizenPlanningProject.NewTripContentDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ReizenPlanningProject"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Add a new trip"
    PrimaryButtonText="Add"
    SecondaryButtonText="Cancel"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick">

    <StackPanel>
        <TextBlock Margin="0 5" FontSize="18">Destination</TextBlock>
        <TextBox Margin="0 5" PlaceholderText="Destination" x:Name="DestinationNameBox" TextWrapping="Wrap"/>
        <TextBlock Margin="0 5" FontSize="18">Departure date and time</TextBlock>
        <DatePicker Margin="0 5" x:Name="DepartureDatePicker" VerticalAlignment="Top"/>
        <TimePicker Margin="0 5" x:Name="DepartureHourPicker" VerticalAlignment="Top"></TimePicker>
        <TextBlock Margin="0 5" FontSize="18">Return date</TextBlock>
        <DatePicker Margin="0 5" x:Name="ReturnDatePicker" VerticalAlignment="Top"/>
        <TimePicker Margin="0 5" x:Name="ReturnHourPicker" VerticalAlignment="Top"></TimePicker>
    </StackPanel>
</ContentDialog>
